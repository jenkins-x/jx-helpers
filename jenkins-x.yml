buildPack: git
pipelineConfig:
  pipelines:
    pullRequest:
      pipeline:
        stages:
          - agent:
              image: golang:1.15
            name: cli
            steps:
              - name: test
                command: make test